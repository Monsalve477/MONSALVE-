

x <- "hola"
y <- "UNTRM"

paste(x,y)


saludo<- function(z,f){
  
  
  paste(z,f)
  
  
}


saludo(z= "hola",f= "FICA")



# mi primera function

area<-function(radio) {
  pi*radio*radio
  
}


area(radio =7)

area(radio = "a")


area_circunferencia<-function(radio){
  
  if(!is- numeric(radio)){
    
    stop("El valor del radio debe ser numérico")
  }
  if(radio<0){
    stop("El radio no puede ser negativo")
  }

  area<-pi*radio2
  
  
  
  
  
  
  # Función para calcular el Índice de Masa Corporal (IMC)
  calcular_IMC <- function(peso, altura) {
    IMC <- peso / (altura^2)
    return(IMC)
  }
  
  # Datos
  peso <- 65  # en kg
  altura <- 1.62  # en metros
  
  # Calcular el IMC
  IMC <- calcular_IMC(peso, altura)
  
  # Imprimir el resultado
  paste("Tu Índice de Masa Corporal (IMC) es:", round(IMC, 2))
  